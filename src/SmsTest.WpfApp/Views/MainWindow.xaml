<Window x:Class="SmsWpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Редактор переменных среды" 
        Height="450" 
        Width="950"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style TargetType="TextBox" x:Key="EditableTextBox">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <Style TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="MinHeight" Value="35"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Переменные среды" 
                   FontSize="18" 
                   FontWeight="Bold" 
                   Margin="0,0,0,10"/>

        <ListView Grid.Row="1" 
                  ItemsSource="{Binding Variables}" 
                  Margin="0,0,0,10"
                  BorderBrush="#CCCCCC"
                  BorderThickness="1">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Имя переменной" Width="200">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Name}" 
                                           VerticalAlignment="Center"
                                           Margin="10,0,0,0"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <GridViewColumn Header="Значение" Width="300">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" 
                                         Style="{StaticResource EditableTextBox}"
                                         BorderBrush="#CCCCCC"
                                         BorderThickness="0,0,0,1"
                                         Margin="5,5,5,5"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Новая колонка для комментариев -->
                    <GridViewColumn Header="Комментарий" Width="400">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding Comment, UpdateSourceTrigger=PropertyChanged}" 
                                         Style="{StaticResource EditableTextBox}"
                                         BorderBrush="#CCCCCC"
                                         BorderThickness="0,0,0,1"
                                         Margin="5,5,5,5"
                                         AcceptsReturn="True"
                                         TextWrapping="Wrap"
                                         MinHeight="30"
                                         MaxHeight="60"
                                         VerticalScrollBarVisibility="Auto"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="Сохранить изменения" 
                    Padding="15,8" 
                    Margin="0,0,10,0"
                    Click="Save_Click"
                    Background="#007ACC"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"
                    FontWeight="SemiBold">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="3"/>
                    </Style>
                </Button.Resources>
            </Button>

            <Button Content="Обновить" 
                    Padding="15,8" 
                    Click="Refresh_Click"
                    Background="#6C757D"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"
                    FontWeight="SemiBold">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="3"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>
    </Grid>
</Window>